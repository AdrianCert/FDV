@model IEnumerable<Data.Exam>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Gfest/Views/Shared/_gfest.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Verdict)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RaspunsuriCorecte)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ColectData)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Verdict)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RaspunsuriCorecte)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ColectData)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
